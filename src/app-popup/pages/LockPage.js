import React, { Component } from 'react';
import LockPageContainer from 'app-popup/containers/LockPageContainer'

class LockPage extends Component {
  render() {
    return (
      <div>
        <LockPageContainer />
      </div>
    );
  }
}

export default LockPage;
